# log

- in the filename field when you create a new file, start typing the folder name you want; when you hit the `/` character, github will know that that was a folder, thus typing `week1/log.md` created a folder with the file log.md in it.
